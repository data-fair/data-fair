// Index tabular datasets with elasticsearch using available information on dataset schema

const indexDatasets = module.exports = async function(db, es) {
  setTimeout(() => indexDatasets(db, es), 1000)
}
