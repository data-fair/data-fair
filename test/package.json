{
  "name": "@data-fair/data-fair-test",
  "type": "module",
  "scripts": {
    "test": "NODE_CONFIG_DIR=../api/config/ NODE_ENV=test NODE_OPTIONS=--experimental-strip-types EVENTS_LOG_LEVEL=warn mocha --exit --bail --timeout 30000 ./"
  },
  "devDependencies": {
    "mocha": "^11.1.0",
    "nock": "^13.5.6",
    "tableschema": "^1.12.6"
  }
}
