version: '3'
services:
  site:
    image: jekyll/minimal:3.6
    command: jekyll serve --incremental
    volumes:
      - $PWD:/srv/jekyll
      - $PWD/vendor/bundle:/usr/local/bundle
    ports:
      - 4000:4000
      - 35729:35729
      - 3000:3000
